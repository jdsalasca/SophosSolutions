<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sophossolutions">

    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- To request foreground location access, declare one of these permissions. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Required only when requesting background location access on Android 10 (API level 29). -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />


    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

<!--For login with Biometrics-->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!--For check internet connection -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />


    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_baseline_air"
        android:label="Sophos Solutions"
        android:roundIcon="@drawable/ic_baseline_air"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.App.Starting">
        <meta-data android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB-mww6GnsU6QAnm-rQ74CysACc3WkuHRI"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:usesCleartextTraffic="true"
            android:windowSoftInputMode="adjustPan"
            android:label="Sophos Solutions"
            android:theme="@style/Theme.SophosSolutions">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>